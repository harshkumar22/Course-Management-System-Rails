<h1>Welcome <%= @user.uname %> </h1>
<div class="container-button">
    <span class="button"><%= link_to "Edit Profile", edit_user_path%></span>
    <span class="button"><%= link_to "Publish a Course", new_course_path %></span>
    <span class="button"><%= link_to "Buy a Course", new_course_path %></span>
    <span class="button"><%= link_to "Log Out", logout_url %></span>
</div>
<div>
    <h2>Course Bought</h2>
    <table>
        <tr>
            <th>Course Name</th>
            <th>Course Price</th>
            <th>Valid Till</th>
        </tr>
        <tr>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </table>
</div>
<div>
    <h2>Course Published</h2>
    <table>
        <tr>
            <th>Course Name</th>
            <th>Course Rating</th>
            <th>Enrollemnt Count</th>
        </tr>
        
        <% if (@course_publishe.class)  %>
            <% (@course_publishe).each do |i|%>
                <tr> 
                    <td><%= Course.find(i.course_id).cname %></td>
                    <td><%= Course.find(i.course_id).rating %></td>
                    <td>1</td>
                </tr>
            <% end %>
        <% end %>
    </table>
</div>